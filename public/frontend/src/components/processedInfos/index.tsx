import {processedInfo} from '../../utils/types';

interface Props {
    processedInfos: processedInfo[];
}

function ProcessedInfos (props: Props) {
    return (
        <div></div>
    )
};

export default ProcessedInfos;
